services:
  app:
    build: .
    image: assembly8-review-finder
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      # .env 가 없을 때를 대비한 안전 기본값(데모 모드)
      DEMO_MODE: ${DEMO_MODE:-1}
    restart: unless-stopped
